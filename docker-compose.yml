version: '3.8'

# ChronicleLedger Infrastructure
# Production-ready setup with Event Sourcing + CQRS

services:
  # ============================================
  # PRIMARY DATABASE (Event Store + Read Model)
  # ============================================

  postgres:
    image: postgres:16-alpine
    container_name: chronicle-db
    environment:
      POSTGRES_USER: chronicle
      POSTGRES_PASSWORD: chronicle
      POSTGRES_DB: chronicle
      POSTGRES_HOST_AUTH_METHOD: md5 # Use MD5 instead of scram-sha-256
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
      - ./infra/postgres-combined/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - chronicle-net
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U chronicle" ]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    command: >
      postgres -c listen_addresses='*' -c password_encryption=md5

  # ============================================
  # MESSAGE BUS (Event Distribution)
  # ============================================

  nats:
    image: nats:2.10-alpine
    container_name: chronicle-nats
    command: [ "-js", "-sd", "/data" ]
    ports:
      - "4222:4222" # Client connections
      - "8222:8222" # HTTP monitoring
    volumes:
      - nats-data:/data
    networks:
      - chronicle-net
    healthcheck:
      test: [ "CMD", "wget", "--spider", "-q", "http://localhost:8222/healthz" ]
      interval: 5s
      timeout: 3s
      retries: 3
    restart: unless-stopped

# ============================================
# VOLUMES
# ============================================

volumes:
  db-data:
    name: chronicle-db-data
  nats-data:
    name: chronicle-nats-data

# ============================================
# NETWORKS
# ============================================

networks:
  chronicle-net:
    name: chronicle-network
    driver: bridge
